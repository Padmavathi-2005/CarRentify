import ProfileView from "@/views/ProfileView";

export const metadata = {
 title: "Profile Settings | CarRental",
 description: "Manage your account identity, security, and verification artifacts.",
};

export default function ProfilePage() {
 return <ProfileView />;
}
